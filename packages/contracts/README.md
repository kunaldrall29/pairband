# @pairband/contracts

P0 does **not** require a custom Pairband contract when using ERC-20 transfer + Arc Memo.

P2+: optional `PairbandPay` multicall / Uniswap v4 hook for atomic exact-out + pay + memo. Never hold user funds in a Pairband vault in v1.

Foundry scaffold reserved for that phase. Do not start hook work while Pay USDC is unfinished.
