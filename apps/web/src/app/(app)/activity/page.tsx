import { RoutePlaceholder } from "../../../components/RoutePlaceholder";

export default function ActivityPage() {
  return (
    <RoutePlaceholder
      title="Activity"
      purpose="Confirmed events and exports. Quote records are never shown as fills."
    />
  );
}
