import { RoutePlaceholder } from "../../../components/RoutePlaceholder";

export default function SettingsPage() {
  return (
    <RoutePlaceholder
      title="Settings"
      purpose="Wallet session, reminder preferences, and display timezone. Reminders require verified email and a configured provider."
    />
  );
}
