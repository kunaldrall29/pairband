import { RoutePlaceholder } from "../../../components/RoutePlaceholder";

export default function StatusPage() {
  return (
    <RoutePlaceholder
      title="Status"
      purpose="Network, indexer, and integration availability. Graph and email are optional and disabled by default."
    />
  );
}
