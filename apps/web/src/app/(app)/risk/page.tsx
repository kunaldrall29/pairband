import { RoutePlaceholder } from "../../../components/RoutePlaceholder";

export default function RiskPage() {
  return (
    <RoutePlaceholder
      title="Risk"
      purpose="Product risks and contract authority map. Unaudited until independent review (O28)."
    />
  );
}
