import { RoutePlaceholder } from "../../../components/RoutePlaceholder";

export default function FundPage() {
  return (
    <RoutePlaceholder
      title="Fund"
      purpose="Optional USDC funding. Circle App Kit is not required. Manual instructions always available."
    />
  );
}
