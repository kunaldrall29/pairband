import { RoutePlaceholder } from "../../../components/RoutePlaceholder";

export default function EarnPage() {
  return (
    <RoutePlaceholder
      title="Earn"
      purpose="Write EURC options with USDC backing, then sell for premium. Minting alone earns nothing. Flow arrives in later stages."
    />
  );
}
