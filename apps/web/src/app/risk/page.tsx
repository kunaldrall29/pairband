import { tokens } from '@pairband/ui';

export default function Page() {
  return (
    <main style={{ padding: 32, maxWidth: 720, margin: '0 auto' }}>
      <p style={{ color: tokens.action, fontWeight: 600 }}>Product preview</p>
      <h1 style={{ color: tokens.text }}>/risk</h1>
      <p style={{ color: tokens.muted }}>
        Scaffold only. Functional flow arrives in later O-stages. No fabricated balances or quotes.
      </p>
    </main>
  );
}
